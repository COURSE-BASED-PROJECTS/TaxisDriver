import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  form: {
    flex: 1,
    paddingLeft: 30,
    marginTop: 10
  },
  formElement: {
    flex: 1,
  },
  formElementTitle: {
    fontSize: 16,
  },

  formTextInput: {
    margin: 5,
  },
});

export default styles;
